/**
 * ------------------------------------------------------------------------------
 * Welcome to Blimp@1.0.0! This is the main entry point for your application.
 * 
 * To start, install nodemon globally:
 * npm install -g nodemon
 * 
 * Then, run the following command to start your application:
 * nodemon app.js
 * 
 * You can also run your application without nodemon:
 * node app.js --watch
 * ------------------------------------------------------------------------------
 */

const app = require('./system/core/Blimp');

/**
 * ------------------------------------------------------------------------------
 * Below this line, you can add dependencies to your application.
 * Example:
 * 
 * Install a dependency:
 * npm install dependencyName --save
 * 
 * Add the dependency to your application by adding the following line:
 * app.use('dependencyName'); 
 * ------------------------------------------------------------------------------
 */
